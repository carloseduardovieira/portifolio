import { DynamicControlPipe } from './dynamic-control.pipe';

describe('DynamicControlPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicControlPipe();
    expect(pipe).toBeTruthy();
  });
});
